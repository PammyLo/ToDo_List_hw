import Vue from 'vue';

document.addEventListener('DOMContentLoaded', () => {
  new Vue({
    el: "#app",
    data: {
      actions:[ "Something", "More stuff" ],
      newAction: ""
},
    // methods: {
    //
    // }
  });
});
